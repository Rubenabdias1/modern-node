{
  "name": "modern-node",
  "version": "1.0.0",
  "description": "A simple bootstrap repo for modern node with typescript.",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "start": "node dist",
    "build": "tsc",
    "dev": "node --no-warnings --watch --loader ts-node/esm --experimental-specifier-resolution node src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ruben Nunez",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
